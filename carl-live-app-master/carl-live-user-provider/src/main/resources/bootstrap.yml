spring:
  application:
    name: carl-live-user-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        namespace: carl-live-app
      username: nacos
      password: nacos

