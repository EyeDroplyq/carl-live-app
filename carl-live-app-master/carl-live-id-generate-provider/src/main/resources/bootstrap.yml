spring:
  cloud:
    nacos:
      username: nacos
      password: nacos
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
  config:
    import:
      - optional:nacos:carl-live-id-generate-provider.yaml
  application:
    name: carl-live-id-generate-provider