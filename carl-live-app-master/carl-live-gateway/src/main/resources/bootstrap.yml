spring:
  application:
    name: carl-live-gateway
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848

  config:
    import:
      - optional:nacos:carl-live-gateway.yaml
